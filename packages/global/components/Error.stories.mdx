import { Story, Preview, Meta } from '@storybook/addon-docs/blocks'
import { withKnobs, text, select } from '@storybook/addon-knobs'
import Error from './Error.vue'

<Meta title="Error" component={ Error } decorators={ [withKnobs] } />

# Error

<Description of={ Error } type='docgen' />

## Props

<Props of={ Error } />

## Default

<Preview>
  <Story name="Default">
    {
      {
        components: { Error },
        template: '<div style="height: 200px"><error :message="message" :errorClass="errorClass" /></div>',
        props: {
          message: {
            default: text('message', 'Something went wrong')
          },
          errorClass: {
            default: select('errorClass', { none: null, chart: 'chart' }, null)
          }
        }
      }
    }
  </Story>
</Preview>

## Chart-related error 

<Preview>
  <Story name="Chart-related error">
    {
      {
        components: { Error },
        template: '<div style="height: 200px"><error :message="message" :errorClass="errorClass" /></div>',
        props: {
          message: { default: () => 'Something went wrong' },
          errorClass: { default: () => 'chart' },
        }
      }
    }
  </Story>
</Preview>
