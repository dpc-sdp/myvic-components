import { Meta, Preview, Story, Props, Source, Description } from '@storybook/addon-docs/blocks';
import AreaWithData from './AreaWithData.vue'
import { withKnobs, object, boolean, select } from '@storybook/addon-knobs'
import { RplContainer, RplRow, RplCol } from '@dpc-sdp/ripple-grid'

<Meta title="Examples/AreaWithData" component={ AreaWithData } decorators={ [withKnobs] } />

# AreaWithData

<Description of={ AreaWithData } type='docgen' />

## Installation

```bash
npm install @dpc-sdp/myvic-examples --save
```

## Import

```javascript
import { AreaWithData } from '@dpc-sdp/myvic-examples'
```

## Usage

```html
<area-with-data />
```

## Props

<Props of={ AreaWithData } />

## Default

<Preview>
  <Story name="Default">
    {
      {
        components: { AreaWithData },
        template: '<area-with-data />',
        props: {
        }
      }
    }
  </Story>
</Preview>
