import { Meta, Preview, Story, Props, Source, Description } from '@storybook/addon-docs/blocks';
import AreaWithMap from './AreaWithMap.vue'
import { withKnobs, object, boolean, select } from '@storybook/addon-knobs'
import { RplContainer, RplRow, RplCol } from '@dpc-sdp/ripple-grid'

<Meta title="Examples/AreaWithMap" component={ AreaWithMap } decorators={ [withKnobs] } />

# AreaWithMap

<Description of={ AreaWithMap } type='docgen' />

## Installation

```bash
npm install @dpc-sdp/myvic-examples --save
```

## Import

```javascript
import { AreaWithMap } from '@dpc-sdp/myvic-examples'
```

## Usage

```html
<area-with-map />
```

## Props

<Props of={ AreaWithMap } />

## Default

<Preview>
  <Story name="Default">
    {
      {
        components: { AreaWithMap },
        template: '<area-with-map />',
        props: {
        }
      }
    }
  </Story>
</Preview>
